// one way
// const small = 1;
// const medium = 2;
// const large = 3;

// Pascalcase convention - first letter of every word is uppercase
//Enum 
const enum Size { Small = 1, Medium, Large }; 
let mySize: Size = Size.Medium;
console.log(mySize);